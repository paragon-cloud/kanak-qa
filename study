<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
<p>The Action Kit action named <code>GITHUB_GET_PR_FILES</code> currently displays the label “Get Files Changed in PR,” which is misleading because the action returns all files associated with the pull request rather than only the modified ones; to avoid confusion and better reflect actual behavior, please update the label to a more accurate phrasing such as “Get Files in PR” (or “List PR Files”), ensuring users clearly understand that the action retrieves the complete set of files included in the PR.</p>
